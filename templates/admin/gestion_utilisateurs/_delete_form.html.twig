<form method="post" action="{{ path('app_delete_gestion_utilisateurs', {'id': utilisateur.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
       <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.id) }}">
       <button>Supprimer</button>
</form>