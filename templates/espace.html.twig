<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DC Consult - {% block title %}{% endblock %}</title>

    <link rel="icon" type="image/png" href="{{ asset('images/favicon.png') }}">

    {# Bootstrap 5 #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

    {# Font Awesome 6 #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    {# CSS Inline du Loader (pour éviter le FOUC du loader) #}
    <style>
        @keyframes dc-loader-spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes dc-loader-fade-out {
            0% { opacity: 1; visibility: visible; }
            99% { opacity: 0; visibility: visible; }
            100% { opacity: 0; visibility: hidden; }
        }

        .dc-page-loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #ffffff 0%, #f2f0ef 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            opacity: 1;
            visibility: visible;
        }

        .dc-page-loader.is-hidden {
            animation: dc-loader-fade-out 0.6s cubic-bezier(0.4, 0, 0.2, 1) forwards;
        }

        .dc-loader-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1.5rem;
        }

        .dc-loader-spinner {
            width: 60px;
            height: 60px;
            border: 3px solid rgba(0, 85, 164, 0.15);
            border-top-color: #0055A4;
            border-right-color: #0055A4;
            border-radius: 50%;
            animation: dc-loader-spin 1s cubic-bezier(0.4, 0, 0.6, 1) infinite;
        }

        .dc-loader-text {
            font-family: 'Montserrat', sans-serif;
            font-size: 0.875rem;
            font-weight: 500;
            color: #2d2d2d;
            letter-spacing: 0.3px;
        }

        .dc-loader-text span {
            display: inline-block;
            animation: dc-loader-blink 1.4s ease-in-out infinite;
        }

        .dc-loader-text span:nth-child(1) { animation-delay: 0s; }
        .dc-loader-text span:nth-child(2) { animation-delay: 0.2s; }
        .dc-loader-text span:nth-child(3) { animation-delay: 0.4s; }

        @keyframes dc-loader-blink {
            0%, 60%, 100% { opacity: 0.5; }
            30% { opacity: 1; }
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>

<body>

{# Page Loader #}
<div class="dc-page-loader" id="pageLoader">
    <div class="dc-loader-content">
        <div class="dc-loader-spinner"></div>
        <div class="dc-loader-text">
            <span>.</span><span>.</span><span>.</span>
        </div>
    </div>
</div>

<nav class="dc-navbar navbar navbar-expand-lg fixed-top" id="mainNavbar">
    <div class="container-fluid px-4">

        <a href="{{ path('app_home') }}" class="dc-navbar__brand navbar-brand">
            <img src="{{ asset('images/logo.png') }}" alt="DC Consult" class="dc-navbar__logo">
        </a>

        <div class="d-flex align-items-center gap-3 ms-auto">
            {# Sidebar toggle pour mobile (visible qu'à partir de lg) #}
            <button type="button"
                    class="dc-navbar__sidebar-btn"
                    id="sidebarToggler"
                    aria-label="Ouvrir la navigation"
                    title="Navigation">
                <i class="fa-solid fa-compass"></i>
            </button>

            {# Navbar burger menu #}
            <button class="dc-navbar__menu-toggle"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbarEspaceContent"
                    aria-controls="navbarEspaceContent"
                    aria-expanded="false"
                    aria-label="Ouvrir le menu"
                    id="navbarToggler">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>

        <div class="collapse navbar-collapse" id="navbarEspaceContent">

            <ul class="navbar-nav mx-auto align-items-lg-center gap-lg-1">
                <li class="nav-item">
                    <a href="{{ path('app_home') }}"
                       class="dc-navbar__link {% if app.request.attributes.get('_route') == 'app_home' %}is-active{% endif %}">
                        Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_projets') }}"
                       class="dc-navbar__link {% if app.request.attributes.get('_route') == 'app_projets' %}is-active{% endif %}">
                        Projets
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_clients') }}"
                       class="dc-navbar__link {% if app.request.attributes.get('_route') == 'app_clients' %}is-active{% endif %}">
                        Clients
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_logiciels') }}"
                       class="dc-navbar__link {% if app.request.attributes.get('_route') == 'app_logiciels' %}is-active{% endif %}">
                        Logiciels
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_contact') }}"
                       class="dc-navbar__link {% if app.request.attributes.get('_route') == 'app_contact' %}is-active{% endif %}">
                        Contact
                    </a>
                </li>
            </ul>

            <div class="d-flex align-items-center gap-2 mt-3 mt-lg-0">
                <div class="dc-navbar__user">
                    <span class="dc-navbar__user-avatar">
                        <i class="fa-solid fa-user"></i>
                    </span>
                    <span class="dc-navbar__user-name">
                        {{ app.user.prenom ?? app.user.userIdentifier }}
                    </span>
                    {% if is_granted('ROLE_ADMIN') %}
                        <span class="dc-navbar__role-badge">Admin</span>
                    {% endif %}
                    <span class="dc-navbar__user-sep"></span>
                    <form method="post" action="{{ path('app_deconnexion') }}" class="d-inline m-0 p-0">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                        <button type="submit" class="dc-navbar__btn-logout">
                            <i class="fa-solid fa-arrow-right-from-bracket fa-sm"></i>
                            <span class="d-none d-lg-inline">Déconnexion</span>
                        </button>
                    </form>
                </div>

        </div>
    </div>
</nav>

<div class="dc-layout">

    <aside class="dc-sidebar" aria-label="Navigation de l'espace personnel">

        {% if is_granted('ROLE_ADMIN') %}
            <div class="dc-sidebar__admin-badge">
                <span class="dc-sidebar__admin-dot"></span>
                <span>Accès administrateur</span>
            </div>
        {% endif %}

        <span class="dc-sidebar__label">Mon espace</span>

        <a href="{{ path('app_dashboard') }}"
           class="dc-sidebar__link {% if app.request.attributes.get('_route') == 'app_dashboard' %}is-active{% endif %}">
            <i class="fa-solid fa-gauge dc-sidebar__link-icon"></i>
            Tableau de bord
        </a>

        <a href="{{ path('app_mes_documents') }}"
           class="dc-sidebar__link {% if app.request.attributes.get('_route') starts with 'app_mes_documents' %}is-active{% endif %}">
            <i class="fa-solid fa-file-lines dc-sidebar__link-icon"></i>
            Mes documents
        </a>

        <a href="{{ path('app_compte') }}"
           class="dc-sidebar__link {% if app.request.attributes.get('_route') == 'app_compte' %}is-active{% endif %}">
            <i class="fa-solid fa-circle-user dc-sidebar__link-icon"></i>
            Mon compte
        </a>

        {% if is_granted('ROLE_ADMIN') %}
            <span class="dc-sidebar__label">Administration</span>

            <a href="{{ path('app_dashboard_admin') }}"
               class="dc-sidebar__link {% if app.request.attributes.get('_route') == 'app_dashboard_admin' %}is-active{% endif %}">
                <i class="fa-solid fa-gauge-high dc-sidebar__link-icon"></i>
                Dashboard admin
            </a>

            <a href="{{ path('app_gestion_utilisateurs') }}"
               class="dc-sidebar__link {% if app.request.attributes.get('_route') starts with 'app_gestion_utilisateurs' %}is-active{% endif %}">
                <i class="fa-solid fa-users dc-sidebar__link-icon"></i>
                Utilisateurs
            </a>

            <a href="{{ path('app_gestion_projets') }}"
            class="dc-sidebar__link {% if app.request.attributes.get('_route') starts with 'app_gestion_projets'
                or app.request.attributes.get('_route') starts with 'app_new_gestion_projets'
                or app.request.attributes.get('_route') starts with 'app_edit_gestion_projets'
                or app.request.attributes.get('_route') starts with 'app_show_gestion_projets' %}is-active{% endif %}">
                <i class="fa-solid fa-folder-open dc-sidebar__link-icon"></i>
                Projets
            </a>

            <a href="{{ path('app_gestion_documents') }}"
               class="dc-sidebar__link {% if app.request.attributes.get('_route') == 'app_gestion_documents' %}is-active{% endif %}">
                <i class="fa-solid fa-file-export dc-sidebar__link-icon"></i>
                Documents
            </a>
        {% endif %}

    </aside>

    <div class="dc-sidebar-overlay" id="sidebarOverlay"></div>

    <main class="dc-main">
        {% block body %}{% endblock %}
    </main>

</div>

{# Bootstrap JS #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

{# AssetMapper — charge app.js et app.css automatiquement #}
{{ importmap('app') }}

{# Masquer le loader dès que le DOM est rendu #}
<script>
    (function hideLoader() {
        const pageLoader = document.getElementById('pageLoader');
        if (!pageLoader) return;
        
        pageLoader.classList.add('is-hidden');
        setTimeout(() => {
            if (pageLoader && pageLoader.parentNode) {
                pageLoader.remove();
            }
        }, 600);
    })();
</script>

{% block javascripts %}{% endblock %}

</body>
</html>
